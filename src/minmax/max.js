/**
 * returns the maximum element of array of numbers
 * @module order-statistics/Max
 * @type {Function}
 * @param {Array} list list of Numbers
 * @returns {Number} maximum Number
 */
module.exports = (list) => Math.max(...list)
